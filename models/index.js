const User = require("./user-model.js"); // Importing the User model
const Thought = require("./thought-model.js"); // Importing the Thought model

module.exports = { User, Thought }; // export the User and Thought model